# /git-scorecard

Run the repo-scorecard skill on this repository.

Read and follow every step in `.claude/skills/repo-scorecard/SKILL.md` exactly as written. Execute all discovery commands, score each dimension using the criteria defined in the skill, then write `repo-scorecard.md` at the repo root.

Do not ask for input. Do not skip steps. Produce the full report.
