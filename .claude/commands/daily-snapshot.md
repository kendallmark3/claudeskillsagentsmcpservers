# /daily-snapshot

Run the daily delivery snapshot skill on this repository.

Read and follow every step in `.claude/skills/daily-snapshot/SKILL.md` exactly as written. Execute all discovery commands, classify findings into Shipped / In Flight / At Risk / Needs Decision, then write `daily-snapshot.md` at the repo root.

Write for a manager audience — plain English, no diffs, no code blocks in the output. A VP should be able to read it in 90 seconds.

Do not ask for input. Do not skip steps. Produce the full report.
